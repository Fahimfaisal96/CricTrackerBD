<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.abc.crictrackerbd">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".editFixture"></activity>
        <activity android:name=".addFixture" />
        <activity android:name=".showSingleFixture" />
        <activity android:name=".showDateList" />
        <activity android:name=".showFixtureFormat" />
        <activity android:name=".showLivescore" />
        <activity android:name=".showPlayers" />
        <activity android:name=".showScorecard" />
        <activity android:name=".showFixtures" />
        <activity android:name=".resetPasswordPage" />
        <activity android:name=".registerUser" />
        <activity android:name=".FrontPage" />
        <activity android:name=".LoginPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>